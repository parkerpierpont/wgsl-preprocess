use super::AnilineDriver;
use skia_safe::Surface;
pub struct Cpu;

// impl AnilineDriver for Cpu {
//     fn new_surface() -> skia_safe::Surface {
//         Surface::new_raster_direct(image_info, pixels, row_bytes, surface_props)
//     }
// }
